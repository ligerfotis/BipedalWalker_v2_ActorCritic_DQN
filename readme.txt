######################################
##########  Instructions #############
######################################

1. You have to run 'helper.py' at least 1 time
to produce the training data. 

2. In the helper.py you can change the pre -training
parameters 'score_requirement'(events with reward bellow
that are being rejected) and 'pretrain_length'(the # of events to be stored)

You increasing both will lead to good pre training examples, but it will take time

3. 
